<div class="tile tile-primary">
    <div class="tile-heading">{{ heading_title }}</div>
    <div class="tile-body"><i class="fa fa-envelope-o"></i>
        <h2 class="pull-right">{{ total_open }}</h2>
        <p style="margin: -24px 0px;">{{ text_total_all }} -  {{ total_all }}  </p>
    </div>
    <div class="tile-footer"><a href="{{ online }}">{{ text_view }}</a></div>
</div>

